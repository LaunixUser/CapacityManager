plugins {
    id 'java'
}

group 'org.ml'
version '2.0.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile (
        [group: 'org.ml', name: 'process-framework', version: '[2.0,)']
    )
}
